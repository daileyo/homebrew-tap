# homebrew-tap

Homebrew tap for [omgitworks](https://github.com/daileyo/omgitworks) — a lightweight,
cross-platform CLI for discovering, organizing, and navigating git repositories.

## Install

```sh
brew install daileyo/tap/omgitworks
```

Or tap first:

```sh
brew tap daileyo/tap
brew trust daileyo/tap
brew install omgitworks
```

Homebrew 7.0 refuses to load formulae from an untrusted third-party tap, so the `brew trust`
step is required when installing by bare formula name. The one-line form above does not need
it — naming the tap explicitly trusts that formula for you.

## Upgrading from the old tap

The project was formerly `git-workspace`, distributed from `daileyo/homebrew-gws`:

```sh
brew uninstall git-workspace
brew untap daileyo/gws
brew install daileyo/tap/omgitworks
```

The `omgw` command replaces `gws`. Shell integration still defines `gws` as a wrapper, so
existing shell configuration keeps working.

## Contents

`Formula/omgitworks.rb` is generated by [GoReleaser](https://goreleaser.com) on each release
and installs a prebuilt binary. Do not edit it by hand.
